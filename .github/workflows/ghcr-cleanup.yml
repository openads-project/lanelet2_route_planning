name: ghcr-cleanup

on:
  schedule:
    - cron: "0 0 * * 0" # every Sunday at 00:00
  workflow_dispatch:
  push: # TODO: remove

jobs:
  ghcr-cleanup:
    uses: openads-project/openads-dev-environment/.github/workflows/ghcr-cleanup.yml@helm # TODO: main
    secrets: inherit
